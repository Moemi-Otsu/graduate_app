          <!-- お気に入り begin -->
          <% @like = current_user.likes.find_by(talk_id: t.id) %>
          <li>
            <% unless t.user_id == current_user.id %>
              <% if @like.present? %>
                <%= link_to 'お気に入り解除する', like_path(id: @like.id), method: :delete, remote: true, class: 'btn btn-danger' %>
              <% else %>
                <%= link_to 'お気に入りする', likes_path(talk_id: t.id), method: :post, remote: true, class: 'btn btn-primary' %>
              <% end %>
            <% end %>
          </li>
          <!-- お気に入り end -->